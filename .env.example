# Base de données (SQLite en dev, passer à une URL postgresql:// en prod
# après avoir changé le provider dans prisma/schema.prisma)
DATABASE_URL="file:./dev.db"

# Clé API Anthropic (https://console.anthropic.com/), utilisée pour le
# regroupement automatique des questions par IA (lib/clustering.ts)
ANTHROPIC_API_KEY=""

# Base de données PostgreSQL (Neon en dev comme en prod, cf. prisma/schema.prisma)
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# Clé API Anthropic (https://console.anthropic.com/), utilisée pour le
# regroupement automatique des questions par IA (lib/clustering.ts)
ANTHROPIC_API_KEY=""
